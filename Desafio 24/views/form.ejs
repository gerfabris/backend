<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Deno</title>
</head>
<body>
    <h1>Desafío 24 - Servidor en Deno</h1>
    <form action="/" method="POST">
        <input type="text" name="color" placeholder="colour" />
        <button type="submit">Enviar</button>
    </form>
    <ul>
        <% if(colors) {%> 
            <% colors.forEach(color => { %>
                <% if(color === "black") { %>
                    <li style="background-color: whitesmoke; color: <%= color %>"> <%= color %> </li>
                <% } else {%>
                    <li style="background-color: rgb(0, 0, 0); color: <%= color %>"><%= color %></li>
                <% } %>
            <% }) %>
        <% } %>
    </ul>
</body>
</html>

<body>
